#include "Scene.hpp"

namespace App
{
    Scene::Scene(entt::registry &registry)
        : IModule(registry)
    {
    }

    Scene::~Scene()
    {
    }

    void Scene::init()
    {
    }

    void Scene::update()
    {
    }

}